# Contributing

1. Create a focused branch.
2. Install development dependencies with `python -m pip install -e ".[dev]"`.
3. Add regression tests for behavior changes and security fixes.
4. Run `pytest` and `ruff check .`.
5. Document changes to payload formats or the threat model.

Security reports must follow `SECURITY.md` rather than public issues.
